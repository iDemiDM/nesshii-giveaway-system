# .env.example - Copy this to .env and fill in your values

# Twitch API Configuration
TWITCH_WEBHOOK_SECRET=your-super-secret-webhook-key-123-change-this
TWITCH_CLIENT_SECRET=your-twitch-client-secret-from-dev-console

# CORS Configuration (comma-separated list of allowed origins)
ALLOWED_ORIGINS=https://your-frontend-domain.com,http://localhost:3000,http://localhost:8080

# Server Configuration
NODE_ENV=development
PORT=3000

# Optional: Database Configuration (if using persistent storage)
# DATABASE_URL=postgresql://user:password@localhost:5432/giveaway_db

# Optional: Redis Configuration (for session storage)
# REDIS_URL=redis://localhost:6379

# Optional: Logging Configuration
# LOG_LEVEL=info
# LOG_FILE=./logs/app.log

# Security Configuration
# RATE_LIMIT_MAX=100
# RATE_LIMIT_WINDOW_MS=900000

# ==================================================
# IMPORTANT SECURITY NOTES:
# ==================================================
# 1. NEVER commit the actual .env file to GitHub
# 2. Use strong, random values for secrets
# 3. Different values for development/production
# 4. Store production secrets in your hosting platform's environment variables
# 5. The TWITCH_WEBHOOK_SECRET should be at least 32 characters long
# ==================================================

# Generate a random webhook secret with this command:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
